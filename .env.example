# Database Configuration (REQUIRED for database features)
# Add a PostgreSQL database in Railway and it will set this automatically
DATABASE_URL="postgresql://username:password@host:5432/database"
DIRECT_URL="postgresql://username:password@host:5432/database"

# Authentication (REQUIRED)
JWT_SECRET="change-this-to-a-secure-random-string-in-production"
NEXTAUTH_SECRET="change-this-to-a-secure-random-string-in-production"

# Application Configuration (REQUIRED)
NEXTAUTH_URL="http://localhost:3000"
APP_URL="http://localhost:3000"
NODE_ENV="development"

# Stripe Payment Configuration (OPTIONAL - required for payment features)
# Get these from https://dashboard.stripe.com/apikeys
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Notes:
# - DATABASE_URL is optional at startup but required for database features
# - The application will start and show an error message if DATABASE_URL is not set
# - In Railway, add a PostgreSQL database and it will automatically set DATABASE_URL
# - Change all secrets to secure random strings in production
# - Never commit actual secrets to version control
